#include <bits/stdc++.h>
using namespace std;

int main()
{
    // int num = 5;
    // int *p = &num;

    // // or
    // int num1 = 6;
    // int *p1 = 0;
    // p = &num1;

    // & --> address
    // &num --> gives address of num
    // *p --> gives value present at address stored in pointer (Same as num)
    // p --> gives address of num (Same as &num)
    // Return Type of p == Return Type of num
    // Size of pointer is 8 byte always
    // cout << &num;
    // cout << p;

    // Integer Array
    // int arr[10] = {2, 4, 6};

    // cout << "Address of arr is " << arr << endl;
    // cout << "Address of arr[1] is " << &arr[1] << endl;
    // cout << "Value at arr is " << *arr << endl;
    // cout << "Value at arr + 1 is " << *arr + 1 << endl;
    // cout << "Value at arr[1] is " << *(arr + 1) << endl;
    // cout << "Value at arr[1] is " << 2[arr] << endl;
    // arr[i] = *(arr + i)
    //        OR
    // i[arr] = *(i + arr)


    // Character Array

    // char ch[6] = "abcde";

    // cout << "Print content of character array --> " << ch << endl;

    // char *c = &ch[0];

    // cout << "Print entire content of character array --> " << c << endl;

    // char ch1 = 'z';
    // char *p = &ch1;
    
    // cout << p << endl;  // prints value at ch1 till then if \0 is encountered



    // Pointers & Functions
    



    return 0;
}